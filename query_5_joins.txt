SELECT OUTER_BUCKET_NAME.* FROM BUCKET_NAME INNER JOIN BUCKET_NAME ON ( CURRENT_TABLE.FIELD = PREVIOUS_TABLE.FIELD ) FOR PREVIOUS_TABLE WHERE NUMERIC_FIELD <= NUMERIC_VALUE;
SELECT OUTER_BUCKET_NAME.* FROM BUCKET_NAME LEFT JOIN BUCKET_NAME ON ( CURRENT_TABLE.FIELD = PREVIOUS_TABLE.FIELD ) FOR PREVIOUS_TABLE WHERE NUMERIC_FIELD BETWEEN LOWER_BOUND_VALUE and UPPER_BOUND_VALUE;
SELECT OUTER_BUCKET_NAME.* FROM BUCKET_NAME INNER JOIN BUCKET_NAME ON ( CURRENT_TABLE.FIELD = PREVIOUS_TABLE.FIELD ) FOR PREVIOUS_TABLE WHERE NUMERIC_FIELD >= NUMERIC_VALUE;
SELECT OUTER_BUCKET_NAME.* FROM BUCKET_NAME LEFT JOIN BUCKET_NAME ON ( CURRENT_TABLE.FIELD = PREVIOUS_TABLE.FIELD ) FOR PREVIOUS_TABLE WHERE NUMERIC_FIELD BETWEEN LOWER_BOUND_VALUE and UPPER_BOUND_VALUE;
SELECT OUTER_BUCKET_NAME.* FROM BUCKET_NAME INNER JOIN BUCKET_NAME ON ( CURRENT_TABLE.FIELD = PREVIOUS_TABLE.FIELD ) FOR PREVIOUS_TABLE WHERE (NUMERIC_FIELD <= NUMERIC_VALUE) OR (NUMERIC_FIELD IN ( LIST ));
